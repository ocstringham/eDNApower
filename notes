to do
- add in readme description of what it does and use correct terminology and use case (hierarch, 2 levels of imperfect detection)
- add methods with equations in readme while make sure formulas are correct (ie actually do what i want it to do)


maybe
- power analysis to get error bars around theta small (see r package that already runs hier edna occ model)
  - would need to take theta and p and simulate sampling efforts while varying s_min. Worth doing just to see how it effects error bars
  - at same time isn't the model output dependent on the param input uncertainty ... so really should compare bwtn input and output uncertainty?
- add for later to do cost surface with costs per sample and tech rep
- Add to do to examine clumped distibs but hold off on that.


no
- Add in prob dna is at the sampling location and assume homogeneous dna throughout site. == not needed bc scope of PA doesn't call for this. often folded into theta


done
- add optional params for ggplot thresholds of power
- go thru roxy documentation to make sure it makes sense
